import { styled } from '@mui/material/styles';
import { Button } from '@mui/material';

export const StyledButtonRadius100 = styled(Button)({
  borderRadius: '100px',
  textTransform: 'none',
  color: 'primary',
});
